{
  "name": "azure-app-configuration-emulator",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build:css": "tailwindcss --input ./Styles/app.css --minify --output ./wwwroot/app.css",
    "format:css": "rustywind --write ."
  },
  "devDependencies": {
    "rustywind": "^0.21.0",
    "tailwindcss": "^3.0.0"
  }
}
